# 環境変数ファイルの例（本番のキーは入れず、env.yaml として保存して --env-vars-file で指定）
# 使い方: cp env.yaml.example env.yaml して値を編集し、
#         gcloud run deploy ... --env-vars-file env.yaml
#
# Cloud Run 用フォーマット（YAML）:
GOOGLE_CLOUD_PROJECT: "picsche-vertex"
VERTEX_LOCATION: "us-central1"
VERTEX_MODEL: "gemini-2.5-pro"
PICSCHE_BACKEND_API_KEY: "ここにAPIキーを入れる"
